# Portfolio Docs Reconciliation

## What changed
Updated portfolio planning and documentation to match the implemented codebase.

## Why
The portfolio transaction MVP was followed by additional implemented work:

- Deutsche Bank Vermögensanlage-Report PDF parsing.
- Snapshot-based lot seeding from broker holdings with cost basis.
- Optional Yahoo-backed security price lookup through an ISIN → ticker map.
- Real-portfolio stop-loss / re-entry scripts and notebook.

The original requirements and implementation docs still described the PDF parser
as deferred and did not mention the later real-portfolio workflows.

## Files affected
- `agent-planning/db-pdf-parser.md` — parser slices marked complete and stale
  "to create" wording updated.
- `docs/portfolio-transaction-mvp/requirements-and-decisions.md` — PDF parser,
  optional price lookup, current workflows, and reverse-split treatment updated.
- `docs/portfolio-transaction-mvp/implementation.md` — parser, snapshot, price
  provider, and real stop-loss workflows documented.
- `agent-planning/reconcile-portfolio-docs.md` — reconciliation plan.

## Validation
Before this reconciliation, the Docker quality command passed:

```text
140 passed
14 files already formatted
All checks passed
```

No Python behavior was changed during the reconciliation.

## Remaining cleanup
The working tree still has unrelated local changes:

- `.claude/settings.local.json` has local Claude permission additions.
- `data/examples/db_holdings_synthetic.normalized.csv` is untracked.
- `data/examples/db_transactions_synthetic.normalized.csv` is untracked.

Decide whether the normalized example CSVs should be committed as reviewable
artifacts or removed/regenerated by the notebook/script workflow.
