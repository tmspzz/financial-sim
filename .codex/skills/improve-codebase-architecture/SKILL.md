---
name: improve-codebase-architecture
description: Use when the user asks for architecture review or when notebooks, shared functions, tests, provider adapters, or portfolio simulation structure need a maintainability critique.
---

# Improve Codebase Architecture

Read `.agents/user-interrogation-skills.md`, then follow the
`/improve-codebase-architecture` workflow.

Prefer consolidating calculation behavior into `src/` and keeping notebooks as
thin reporting layers.
