# Plan: Add inputs display cells to notebooks 01–04

## Goal
Insert a code cell in each of notebooks 01–04, immediately after the `from inputs import` cell,
that renders the live imported values as a labelled Series. Implements IMPROVEMENT_PLAN item 10.

## Slices
- [x] Insert display cell in notebook 01 (position, tax, sensitivity range)
- [x] Insert display cell in notebook 02 (position, tax, stop levels, benchmark tolerance)
- [x] Insert display cell in notebook 03 (position, tax, bear range, recovery formula)
- [x] Insert display cell in notebook 04 (position, tax, bear range, recovery formula, re-entry params)
- [x] Validate notebooks are valid JSON and compile cleanly
