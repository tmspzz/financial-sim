# /tdd

Read `.agents/user-interrogation-skills.md`, then follow the `/tdd` workflow.

Use test-first development for calculation and simulation behavior changes.
Test public shared functions before implementing the smallest passing change.
