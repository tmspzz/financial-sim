services:
  jupyter:
    build:
      context: .
      dockerfile: Dockerfile
    image: financial-sim:latest
    environment:
      PYTHONPATH: /home/jovyan/work/src
    ports:
      - "8888:8888"
    volumes:
      - type: bind
        source: .
        target: /home/jovyan/work
    working_dir: /home/jovyan/work
    command: start-notebook.py --ServerApp.token='' --ServerApp.password=''
